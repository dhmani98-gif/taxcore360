[build]
builder = "nixpacks"

[build.nixpacksConfig]
nodeVersion = "20"

[deploy]
startCommand = "sh -c 'npx vite preview --host 0.0.0.0 --port ${PORT:-3000}'"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"

[phases.build]
cmds = ["npm install", "npm run build"]

[build]
builder = "nixpacks"

[build.nixpacksConfig]
nodeVersion = "20"
# Caddy configuration is in Caddyfile at project root

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
